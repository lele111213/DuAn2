import 'package:flutter/material.dart';
import 'components/body.dart';

class NhatMenhScreen extends StatelessWidget {
  const NhatMenhScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
