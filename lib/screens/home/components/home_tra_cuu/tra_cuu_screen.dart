import 'package:flutter/material.dart';

import 'components/body.dart';

class TraCuuScreen extends StatelessWidget {
  const TraCuuScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
